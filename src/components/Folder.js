import React from 'react';
import './styles/Folder.scss';

const Folder = (folder) => {
  
  return (
    <article className="folder">
      I am a folder
    </article>
  );
};

export default Folder;
